# Database — Redis

Redis *database* concepts: core data structure modeling, secondary indexing and search (RediSearch), JSON documents, time series, probabilistic data structures, geospatial, messaging (queues/Pub-Sub/Streams), vector similarity search, server-side programmability, caching, common architecture patterns (rate limiting, sessions, leaderboards, fraud detection, microservices), transactions/durability/replication consistency, and operations (high availability, cluster sharding, TLS, ACL). Engine-level, language-agnostic. Covers plain Redis and Redis Stack.

## Install

```bash
npx skills add mihailtd/skills/skills/database-redis --all
```

Add `-g` to install globally instead of per-project. Use `--skill <name>` (repeatable) instead of `--all` to cherry-pick individual skills from this package, e.g.:

```bash
npx skills add mihailtd/skills/skills/database-redis --skill database-redis-caching-strategy
```

## Skills (39)

- **[database-redis-stack-overview](database-redis-stack-overview/SKILL.md)** — What Redis Stack is (key-value store → data structure server → multi-model database → data platform) and a capability-to-problem map for routing a requirement to the right skill/module instead of defaulting to plain caching or a second specialized database.
- **[database-redis-environment-setup](database-redis-environment-setup/SKILL.md)** — Installing Redis Stack (Docker image variants, native packages), setting up authenticated client connections for Python/JavaScript-TypeScript/Go, and verifying server readiness with PING-based health checks.
- **[database-redis-om-mapping](database-redis-om-mapping/SKILL.md)** — Using Redis OM (Python's redis-om + Pydantic, JavaScript's redis-om) to map application objects to Redis JSON/Hash documents declaratively — schema definition with indexed fields, migrations, and expression-based querying — instead of hand-writing storage/indexing calls.
- **[database-redis-data-modeling-fundamentals](database-redis-data-modeling-fundamentals/SKILL.md)** — Mapping application objects and collections onto Redis's native data structures (Hash, Set, Sorted Set, List) instead of relational rows or opaque blobs, and efficient primary-key access patterns — with Python, JavaScript/TypeScript, and Go examples.
- **[database-redis-manual-secondary-indexing](database-redis-manual-secondary-indexing/SKILL.md)** — Avoiding full-keyspace SCAN filtering when RediSearch isn't available: pipelining, server-side Lua functions, or hand-rolled Set/Sorted-Set indexes kept consistent with MULTI/EXEC — and recognizing when to stop hand-rolling and adopt RediSearch.
- **[database-redis-search-indexing](database-redis-search-indexing/SKILL.md)** — Creating and querying RediSearch secondary indexes over Hash or JSON documents (TEXT/TAG SORTABLE/NUMERIC SORTABLE fields, combined filters, the FT.AGGREGATE pipeline — GROUPBY/REDUCE/APPLY/FILTER/SORTBY, cursor pagination — for analytics and faceted search, scoped/ephemeral/TEMPORARY indexes, and evolving an index in production with FT.ALTER or an FT.ALIAS zero-downtime swap).
- **[database-redis-search-query-syntax](database-redis-search-query-syntax/SKILL.md)** — Reference for the FT.SEARCH query language: term matching, field modifiers, AND/OR/negation, exact phrases, stop words, prefix/infix/suffix and fuzzy matching, numeric/tag filter syntax, and the DIALECT setting that silently changes how queries parse.
- **[database-redis-search-ux-features](database-redis-search-ux-features/SKILL.md)** — RediSearch search-experience features: result highlighting/summarizing, stemming control (VERBATIM/NOSTEM), synonym groups, spellcheck suggestions, auto-complete with fuzzy matching, and phonetic matching — for handling typos, partial input, and unfamiliar terminology in a search UI.
- **[database-redis-search-relevance-scoring](database-redis-search-relevance-scoring/SKILL.md)** — How RediSearch ranks full-text results: the default TF-IDF scorer, boosting relevance with a rating field via SCORE_FIELD, switching algorithms with SCORER (BM25, DOCSCORE, ...), debugging a ranking with EXPLAINSCORE, and per-field WEIGHT — the content-relevance half of a recommendation feature.
- **[database-redis-json-documents](database-redis-json-documents/SKILL.md)** — Storing and querying nested/hierarchical data with Redis Stack's JSON document type (JSON.SET/JSON.GET, JSONPath array/filter/recursive-descent syntax, JSON.ARRAPPEND/JSON.OBJKEYS), choosing JSON over a flat Hash, and indexing array elements/multi-value fields with RediSearch.
- **[database-redis-time-series](database-redis-time-series/SKILL.md)** — Redis Stack time series: TS.CREATE tuning (RETENTION/ENCODING/CHUNK_SIZE/DUPLICATE_POLICY), atomic counters (TS.INCRBY/DECRBY), label-based multi-series queries (TS.MRANGE/TS.QUERYINDEX), the full aggregation function set, and TS.CREATERULE compaction (including the non-retroactivity gotcha).
- **[database-redis-probabilistic-data-structures](database-redis-probabilistic-data-structures/SKILL.md)** — Trading exact accuracy for memory/speed at scale: HyperLogLog cardinality estimation (PFADD/PFMERGE), Bloom filters for membership/deduplication (BF.ADD/BF.RESERVE error-rate tuning), compact bitfield/bitmap counters and flags, and routing to the right sketch for other question shapes.
- **[database-redis-cuckoo-filter](database-redis-cuckoo-filter/SKILL.md)** — Cuckoo filters (CF.ADD/CF.EXISTS/CF.COUNT/CF.DEL): a fingerprint-bucket alternative to Bloom filters that supports deletion and repeat-item counting, for approximate membership testing where items must be removable.
- **[database-redis-count-min-sketch](database-redis-count-min-sketch/SKILL.md)** — Count-Min Sketch (CMS.INITBYDIM/CMS.INCRBY/CMS.QUERY/CMS.MERGE): approximate per-item frequency counting over a high-volume stream in fixed memory, distinct from HyperLogLog's "how many distinct items" question.
- **[database-redis-topk](database-redis-topk/SKILL.md)** — Top-K (TOPK.RESERVE/TOPK.INCRBY/TOPK.LIST): approximate, fixed-memory tracking of the K highest-frequency items via the HeavyKeeper algorithm, for leaderboards/trending/DDoS detection at scale.
- **[database-redis-tdigest](database-redis-tdigest/SKILL.md)** — t-digest (TDIGEST.CREATE/ADD/QUANTILE/CDF/RANK): quantile/percentile estimation over a data stream in a compact sketch, for latency distributions, scoring thresholds, and anomaly detection.
- **[database-redis-scripting-functions](database-redis-scripting-functions/SKILL.md)** — Redis's single-threaded execution model and why script duration is a shared-server concern, EVAL/EVALSHA/SCRIPT LOAD ad hoc Lua scripting vs. persistent Redis Functions (FUNCTION LOAD/FCALL, libraries, code reuse, no-writes), execution flags (allow-oom/allow-stale/no-cluster/allow-cross-slot-keys), and a Lua-vs-JS comparison table.
- **[database-redis-javascript-functions](database-redis-javascript-functions/SKILL.md)** — Redis Stack JavaScript Functions (TFUNCTION/TFCALL/TFCALLASYNC): library anatomy, calling Redis via the client object, sync vs. async (coroutine) execution and the block() pattern for atomic sections within async, and cluster-aware remote functions (registerClusterFunction/runOnShards/runOnKey).
- **[database-redis-keyspace-triggers](database-redis-keyspace-triggers/SKILL.md)** — registerKeySpaceTrigger for automatic reactions to key changes, and the critical MULTI/EXEC-and-Lua-script trigger-timing gotcha (notifications fire once at EXEC time, seeing only the final value) fixed with onTriggerFired.
- **[database-redis-stream-triggers](database-redis-stream-triggers/SKILL.md)** — registerStreamTrigger: an in-database consumer that processes Stream entries automatically as they arrive (at-least-once on shard failure), versus the client-pulled consumer-group model in database-redis-streams.
- **[database-redis-caching-strategy](database-redis-caching-strategy/SKILL.md)** — Advanced Redis caching strategies: TTL management, jitter to prevent cache stampedes, choosing String vs Hash based on access pattern, and Lua scripting for atomic server-side caching.
- **[database-redis-rate-limiting](database-redis-rate-limiting/SKILL.md)** — Fixed-window counter (INCR/EXPIRE) and sliding-window Sorted Set rate limiters, done atomically, with the tradeoffs (boundary bursts, weighting) between them.
- **[database-redis-session-store](database-redis-session-store/SKILL.md)** — Redis as a centralized session store: why sticky sessions/in-process state break down at scale, framework integrations, choosing Hash vs JSON for session content, modeling rich searchable sessions with JSON+RediSearch (cart/geo/collection queries), multi-object sessions, and atomic cross-key TTL refresh.
- **[database-redis-leaderboards](database-redis-leaderboards/SKILL.md)** — Sorted Set-based leaderboards and rankings: score-ordered range queries, top-N retrieval, rank lookup, and lexicographic ordering when scores tie.
- **[database-redis-geospatial](database-redis-geospatial/SKILL.md)** — Native geospatial commands (GEOADD/GEODIST/GEOSEARCH) for proximity/radius queries, RediSearch's GEO field type for combined location + attribute filtering, and GEOSHAPE for point/polygon area queries (CONTAINS/WITHIN) via Well-Known Text.
- **[database-redis-queues-pubsub](database-redis-queues-pubsub/SKILL.md)** — Lists as FIFO queues, Sorted Sets as priority queues, and Pub/Sub broadcast — and the shared no-acknowledgment limitation that means database-redis-streams is the right call once message loss is unacceptable.
- **[database-redis-streams](database-redis-streams/SKILL.md)** — Durable, replayable, acknowledged messaging with Redis Streams: XADD/XREADGROUP/XACK/XPENDING, consumer groups, and the one-group-per-microservice pattern.
- **[database-redis-vector-similarity-search](database-redis-vector-similarity-search/SKILL.md)** — Storing embeddings in Hash (binary blob) or JSON (array), indexing with the VECTOR field type (FLAT vs HNSW, TYPE/DIM/DISTANCE_METRIC), KNN similarity queries, hybrid pre-filtered queries, and VSS range queries — for recommendation, similarity-matching, and pattern-correlation use cases.
- **[database-redis-llm-integration](database-redis-llm-integration/SKILL.md)** — Redis's three roles in LLM applications: RAG context retrieval, persisting conversation memory as embeddings, and semantic caching of completions by prompt similarity — all built on VSS, with pointers to LangChain/LlamaIndex/Bedrock integrations.
- **[database-redis-fraud-detection](database-redis-fraud-detection/SKILL.md)** — Composing Bloom filters, time series, and vector similarity search into a real-time behavioral-anomaly check (has this user seen this location/time/pattern before).
- **[database-redis-microservices-patterns](database-redis-microservices-patterns/SKILL.md)** — Where Redis Stack fits in microservice architecture: API gateway rate limiting/caching, polyglot persistence, cache-aside query caching, CQRS, and inter-service communication via Streams.
- **[database-redis-primary-database-guarantees](database-redis-primary-database-guarantees/SKILL.md)** — Evaluating Redis as an authoritative primary store: the BASE properties Redis fulfills, a property-by-property ACID mapping, and where the real gaps versus a traditional RDBMS are, with pointers to the transactions/persistence/replication-consistency skills.
- **[database-redis-transactions](database-redis-transactions/SKILL.md)** — MULTI/EXEC/DISCARD, WATCH-based optimistic locking as the actual isolation mechanism, what does and doesn't roll back on a runtime error (partial application by design), and atomic multi-key commands as a lighter alternative.
- **[database-redis-persistence-durability](database-redis-persistence-durability/SKILL.md)** — AOF vs. RDB, the appendfsync policy tradeoff (no/everysec/always) with real throughput/latency numbers, the write(2)/fsync/OS-buffer-cache mechanics, and recovering a corrupted AOF with redis-check-aof.
- **[database-redis-replication-consistency](database-redis-replication-consistency/SKILL.md)** — Why asynchronous replication means a replica can lag behind its master, what that means for data loss on failover, and WAIT/WAITAOF for tightening the guarantee at a real, measurable cost.
- **[database-redis-high-availability](database-redis-high-availability/SKILL.md)** — Configuring replication (replicaof, min-replicas-to-write/min-replicas-max-lag) and Redis Sentinel-managed automatic failover: the odd-quorum requirement, minimum process topology, and self-managed HA's real operational limitations.
- **[database-redis-cluster-sharding](database-redis-cluster-sharding/SKILL.md)** — Redis Cluster horizontal scaling: the 16,384 hash-slot model, CRC16 key-to-slot mapping, cluster creation with replicas, topology inspection (CLUSTER NODES/SHARDS/SLOTS/KEYSLOT), and the no-automatic-rebalancing limitation.
- **[database-redis-tls-security](database-redis-tls-security/SKILL.md)** — Network-level security: never binding Redis to a public interface, TLS encryption for client and internal (replication/cluster) traffic, required certificate files, and mutual TLS (mTLS) as a certificate-based alternative to passwords.
- **[database-redis-access-control](database-redis-access-control/SKILL.md)** — Redis ACL: requirepass/AUTH baseline, ACL SETUSER for per-user restrictions, command categories for coarse-grained rules, and the full rule syntax (command/category allow-deny, key-prefix read/write scoping) for least-privilege access.
