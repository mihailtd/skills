# Python — Core

General-purpose Python skills applicable to almost any Python project: the master `python` skill (stack decisions, PostgreSQL-vs-MongoDB, functional-lite/dataclasses-over-OOP philosophy), project setup, testing, logging, configuration, error handling, functional programming, data structures, async/concurrency (asyncio is standard library, so it lives here rather than in a separate package), and dependency upgrades.

Install this alongside whichever stack-specific package below matches your project (FastAPI, Polars, and one of `python-postgresql`/`python-mongodb`). Start with the `python` skill.

## Install

```bash
npx skills add mihailtd/skills/skills/python-core --all
```

Add `-g` to install globally instead of per-project. Use `--skill <name>` (repeatable) instead of `--all` to cherry-pick individual skills from this package, e.g.:

```bash
npx skills add mihailtd/skills/skills/python-core --skill python
```

## Skills (33)

- **[python](python/SKILL.md)** — General Python programming approach and standards for this project. Defines the core stack, tooling, the PostgreSQL vs MongoDB decision, and the "functional-lite, data classes over OOP" style philosophy. USE THIS SKILL for any Python task. It references specialized domain skills for deeper guidance on specific topics (FastAPI, Alembic, async, functional programming, dependency management).
- **[python-api-pagination-streaming](python-api-pagination-streaming/SKILL.md)** — Instructs the agent on designing robust API pagination avoiding offset pitfalls, using time-based cursors, and implementing memory-safe asynchronous data streaming using database cursors (via asyncpg or SQLAlchemy).
- **[python-architectural-fitness-functions](python-architectural-fitness-functions/SKILL.md)** — Instructs the agent to write automated tests (fitness functions) that verify structural integrity and enforce Clean Architecture's Dependency Rule using Python's ast module.
- **[python-asyncio-concurrent-web-requests](python-asyncio-concurrent-web-requests/SKILL.md)** — Instructs the agent on making non-blocking HTTP requests with aiohttp, and managing concurrent execution using asyncio.gather, asyncio.as_completed, and asyncio.wait, including timeout management.
- **[python-asyncio-synchronization](python-asyncio-synchronization/SKILL.md)** — Instructs the agent on resolving single-threaded concurrency bugs in asyncio, protecting critical sections with Locks, limiting concurrent tasks with Semaphores, and notifying worker tasks with Events and Conditions.
- **[python-concurrent-processing](python-concurrent-processing/SKILL.md)** — Instructs the agent on parallelizing stateless operations using functional map() patterns with ProcessPoolExecutor, ThreadPoolExecutor, and multiprocessing.Pool. Covers integrating CPU-bound work with asyncio via run_in_executor, data partitioning (MapReduce), and managing shared state with locks. Use when parallelizing computations, handling CPU-bound work in async apps, or processing large datasets concurrently.
- **[python-configuration-observability](python-configuration-observability/SKILL.md)** — Instructs the agent on cloud-native application configuration and observability. Covers environment variable configuration (Twelve-Factor methodology), routing logs to stdout, structured JSON logging, hierarchical loggers, cross-boundary request tracing with ContextVar, and framework-independent observability. Use when configuring applications, implementing logging, or adding tracing.
- **[python-dark-mode-light-mode-rollout](python-dark-mode-light-mode-rollout/SKILL.md)** — Implementing the dark-mode/light-mode rollout technique: running an old and new implementation side by side behind a `RolloutMode`-driven dispatcher, comparing outputs while returning the old result (dark) then the new one (light), running I/O-bound implementations concurrently via `asyncio.gather` to avoid doubling latency, and sampling the comparison rate for high-traffic paths.
- **[python-data-structures-type-system](python-data-structures-type-system/SKILL.md)** — Instructs the agent on choosing the right built-in collections (mutable vs. immutable), mandating the typing module for static analysis via `ty`, enforcing Pydantic for run-time data validation, and using Union/Optional syntax, type aliases, NewType, Sequence, and Literal to enforce architectural boundaries. Use when designing data models, defining interfaces, or working with type annotations.
- **[python-domain-error-handling](python-domain-error-handling/SKILL.md)** — Instructs the agent on domain-level error handling, including generic `Result[T]` type patterns (a `Success`/`Failure` tagged union, not an `Any`-typed class), custom exception hierarchies, concealing root causes, and architectural error propagation — use cases are plain functions with injected dependencies, never "interactor" classes.
- **[python-datetime-modeling](python-datetime-modeling/SKILL.md)** — Distinguishing an instant (aware `datetime`), a civil date (`date`, no time zone), a civil datetime bound to a place (naive `datetime` + a separate IANA zone field, resolved at read time), and a duration (`timedelta`) vs. a period (`dateutil.relativedelta`) — the aware-vs-naive trap, IANA zone names vs. offsets/abbreviations, why calendar arithmetic isn't associative or reversible, applying the concept consistently across memory/network/storage, and library selection.
- **[python-datetime-requirements-clarity](python-datetime-requirements-clarity/SKILL.md)** — Treating a vague date/time requirement ("customers can return items within 3 months") as underspecified until five ambiguities are resolved: which instant a period is relative to, which location's time zone governs it (never the server's/database's), the granularity and exact calendar-arithmetic rollover rule, and whether boundaries are inclusive or have a grace period.
- **[python-datetime-testability](python-datetime-testability/SKILL.md)** — Injecting a Clock (`Callable[[], datetime]`, functional-lite — never a class hierarchy) instead of calling `datetime.now()` directly, always passing an explicit IANA zone instead of relying on system-local defaults, avoiding locale-sensitive parsing/formatting for machine-facing text, and splitting a public entry point from a pure, explicitly-parameterized inner function.
- **[python-functional-programming-decorator-design](python-functional-programming-decorator-design/SKILL.md)** — Instructs the agent on creating composite functions using decorators to cleanly handle cross-cutting concerns like null-awareness, bad data filtering, and logging, while strictly mandating the use of functools.wraps.
- **[python-functional-programming-itertools-combinatorics](python-functional-programming-itertools-combinatorics/SKILL.md)** — Instructs the agent on sophisticated iteration using the itertools standard library for grouping, combining, filtering, and safely generating combinatorial products, permutations, and combinations.
- **[python-functional-programming-recursion-tco](python-functional-programming-recursion-tco/SKILL.md)** — Instructs the agent on manually optimizing recursive algorithms into explicit for loops, generator expressions, or using explicit stacks to bypass Python's recursion limits and lack of automatic tail-call optimization.
- **[python-functional-programming-web-services](python-functional-programming-web-services/SKILL.md)** — Instructs the agent on designing FastAPI route handlers as stateless, functional pipelines. It guides decomposing handlers into a cascade of pure functions for validation, data retrieval, and response preparation using Pydantic models and HTTPException.
- **[python-higher-order-functools](python-higher-order-functools/SKILL.md)** — Instructs the agent on using Python's built-in higher-order functions (map, filter, max, min) with lambda forms or pure functions, and the functools module for memoization (@cache, @lru_cache), currying (partial), reductions (reduce), and type-based polymorphism (@singledispatch). Use when processing collections, optimizing functions, or implementing functional design patterns.
- **[python-immutable-data-design](python-immutable-data-design/SKILL.md)** — Instructs the agent on designing immutable data structures and safe resource management. Covers typing.NamedTuple, frozen dataclasses, optional pyrsistent usage, context managers, and the benefits and trade-offs of immutability. Use when designing data containers, value objects, or managing external resources.
- **[python-lazy-evaluation-pipelines](python-lazy-evaluation-pipelines/SKILL.md)** — Instructs the agent to avoid materializing large datasets in memory by prioritizing lazy evaluation. Mandates the use of yield, yield from, generator expressions, stacked generator pipelines, map(), filter(), reduce(), and itertools for functional data processing. Use when processing collections, building data pipelines, or optimizing memory usage.
- **[python-lazy-vs-eager-initialization](python-lazy-vs-eager-initialization/SKILL.md)** — Deciding between lazy and eager initialization for expensive startup resources (DB connection pools, cache warming): eager initialization in a FastAPI lifespan handler pays the cost once and turns a broken dependency into a failed Kubernetes readiness probe that halts a rolling deployment before it reaches real traffic, while lazy initialization defers the cost onto the first N requests (N = pool size) and lets failures go undetected until the app is already serving production traffic.
- **[python-library-creation](python-library-creation/SKILL.md)** — Guides the agent and team on building a modern Python library in 2026. Covers `uv` initialization, src-layout packaging, Astral tooling, type checking with `ty`, testing, CI, pre-commit, and publishing.
- **[python-logging-in-practice](python-logging-in-practice/SKILL.md)** — Instructs the agent on practical logging strategies, including action-oriented log levels, setting up request IDs and correlation via ContextVar, securing audit trails, and integrating with FastAPI middleware.
- **[python-notebooks-marimo](python-notebooks-marimo/SKILL.md)** — Use marimo instead of Jupyter for Python notebooks: reactive execution that eliminates hidden state and out-of-order bugs, storage as git-diffable `.py` files instead of `.ipynb` JSON blobs, instant conversion to interactive web apps, script/CLI execution, and `marimo pair` for live in-notebook AI-agent collaboration.
- **[python-pattern-matching](python-pattern-matching/SKILL.md)** — Instructs the agent on implementing Python 3.10+ structural pattern matching using the match and case statements to cleanly process heterogeneous collections of data, avoiding imperative isinstance() chains.
- **[python-polars-vs-pandas](python-polars-vs-pandas/SKILL.md)** — Use Polars instead of pandas: a Rust-based multi-threaded execution engine vs. pandas' single-threaded eager execution, Apache Arrow memory layout vs. NumPy-array copies, lazy query optimization, larger-than-RAM streaming, and a readable expression-based API.
- **[python-polars-vs-spark](python-polars-vs-spark/SKILL.md)** — Recognizing when a dataset has genuinely outgrown Polars and DuckDB (a difference in kind, not degree — distributed vs. single-machine) and needs Apache Spark: concrete signals to check first, what Spark's data-locality/partitioning/broadcast-join model buys once a machine's limits are real, and why the operational cost of a cluster isn't worth paying for datasets this repository's projects actually deal with.
- **[python-project-setup](python-project-setup/SKILL.md)** — Guides the agent on modern Python project scaffolding using uv, Ruff, Ty, Polars, pyproject.toml, pre-commit enforcement (including type checking), and a GitHub Actions CI/CD workflow enforcing the same checks on every push/PR.
- **[python-pydantic-configuration](python-pydantic-configuration/SKILL.md)** — Instructs the agent on handling application configuration using Pydantic BaseSettings, validating environment variables, managing .env files, and adhering to the Twelve-Factor App methodology.
- **[python-restful-api-design](python-restful-api-design/SKILL.md)** — Instructs the agent on building standardized HTTP interfaces using resources and actions abstractions, proper HTTP methods/status codes, pagination, and the OpenAPI specification.
- **[python-safe-io-file-parsing](python-safe-io-file-parsing/SKILL.md)** — Instructs the agent to strictly use the pathlib module for filesystem operations, parse files non-blockingly using generators, map CSVs directly into dataclasses, and use compiled regular expressions with named capture groups for log parsing.
- **[python-testing-mocking](python-testing-mocking/SKILL.md)** — Instructs the agent on building robust test suites combining pytest, unittest, and doctest. Guides the use of the Arrange-Act-Assert pattern and mandates using unittest.mock.Mock objects and pytest monkeypatch to isolate unpredictable elements like time, randomness, and OS functions.
- **[python-upgrade-dependencies](python-upgrade-dependencies/SKILL.md)** — Upgrade project dependencies in pyproject.toml to their latest available versions, resolved against whatever index is actually configured (public PyPI and/or a private registry). Uses uv to find and apply upgrades, pins exact versions with ==, and is registry-aware — never assumes public PyPI is the source of truth for version resolution. Use when updating dependencies, addressing security vulnerabilities, or performing routine maintenance.
